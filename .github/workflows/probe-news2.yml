name: probe-news2
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - env:
          EODHD_API_TOKEN: ${{ secrets.EODHD_API_TOKEN }}
        run: python3 tools/probe_news.py
